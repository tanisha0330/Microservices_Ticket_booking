apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: ticketflow
  labels:
    app: catalog-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
        - name: catalog-service
          image: ticketflow-catalog-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ticketflow-config
            - secretRef:
                name: catalog-service-secret
          env:
            - name: POSTGRES_DB
              value: "ticketflow_catalog"
            - name: SERVICE_NAME
              value: "catalog-service"
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: ticketflow
spec:
  selector:
    app: catalog-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
